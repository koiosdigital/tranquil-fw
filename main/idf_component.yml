## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: ">=5.5.2"
  espressif/mdns: ^1.9.1
  espressif/esp_websocket_client: ^1.6.1
  # libpng replaced with local spng component (smaller, simpler)
